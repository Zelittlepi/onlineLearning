server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: education-system
  
  # 使用PostgreSQL数据库
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres
    username: postgres.gxauxmtreswttfidxleh
    password: wzsgdsg0031
    driver-class-name: org.postgresql.Driver
    
    # 针对Supabase免费层优化连接池
    hikari:
      maximum-pool-size: 1
      minimum-idle: 0
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
    
  # CORS Configuration
  web:
    cors:
      allowed-origins: "http://localhost:5173"
      allowed-methods: "*"
      allowed-headers: "*"
      allow-credentials: true

# MyBatis Plus Configuration  
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# JWT Configuration
jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000