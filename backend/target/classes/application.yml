server:
  port: 8080

spring:
  application:
    name: education-system
  
  # 使用mySQL数据库
  datasource:
    url: jdbc:mysql://129.204.10.71:3306/online_learning?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: remote_user
    password: Wz20010318.
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    # 针对mysql免费层优化连接池
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
    
  # CORS Configuration
  web:
    cors:
      allowed-origins: "http://localhost:5173"
      allowed-methods: "*"
      allowed-headers: "*"
      allow-credentials: true

# MyBatis Plus Configuration  
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

# JWT Configuration
jwt:
  secret: mySecretKey123456789012345678901234567890
  expiration: 86400000